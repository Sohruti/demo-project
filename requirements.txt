# Web framework
Flask==3.0.0
flask-sqlalchemy==3.1.1

# Database driver for PostgreSQL (needed in production)
# Not used locally with SQLite, but required when DATABASE_URL is a postgres URL
psycopg2-binary==2.9.9

# Production WSGI server — replaces Flask's built-in server
gunicorn==21.2.0
